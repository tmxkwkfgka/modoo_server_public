

type GetMessageUseBytesResponse {
    ok: Boolean!
    error: String
    tx: Int
    rx: Int
}

type Query {
    GetMessageUseBytes(
        whichiId: Int!
        
    ): GetMessageUseBytesResponse!
}