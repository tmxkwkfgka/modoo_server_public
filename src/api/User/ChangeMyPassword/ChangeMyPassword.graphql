type ChangeMyPasswordResponse {
    ok: Boolean!
    error: String
}
  
type Mutation {
    ChangeMyPassword(
        userid: Int!
        previousPassword: String!
        newPassword: String!
        confirmPassword: String!
    ): ChangeMyPasswordResponse!
}
  